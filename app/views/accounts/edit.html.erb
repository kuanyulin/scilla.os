<div class='at-form'>
<fieldset>
  <legend>修改科目</legend>
  
<% form_for(@account) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= f.submit '更新' %>
  </p>
<% end %> 

<%= link_to 'Back', accounts_path %>
