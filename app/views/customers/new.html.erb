<h1>新增客戶</h1>

<div class='at-form'>
<% form_for(@customer) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= f.submit '新增' %>
  </p>
<% end %>
</div>

<%= link_to 'Back', customers_path %>