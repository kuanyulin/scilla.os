<script type="text/javascript">
	var item_price = <%= @style.prices.size != 0 ? @style.prices[@style.prices.length-1].price.to_s : "0" %>
  document.getElementById('order_items_attributes_<%= @temp_record %>_list_price').value = item_price;
	
  var size_options = new Array();
	var size_select =  document.getElementById('order_items_attributes_<%= @temp_record %>_size');
	
	size_select.options.length = 0;

<% for size in @sizes %>
  size_select.options[size_select.options.length] = new Option("<%= size %>", "<%= size %>", false, false);
<% end %>
  
	var list_price = document.getElementById('order_items_attributes_<%= @temp_record %>_list_price');
  var disc_off = document.getElementById('order_items_attributes_<%= @temp_record %>_percent_off');
  var discount = document.getElementById('order_items_attributes_<%= @temp_record %>_discount');
  var subtotal = document.getElementById('order_items_attributes_<%= @temp_record %>_subtotal');
  discount.value = Math.round(list_price.value * disc_off.value / 100);
  subtotal.value = list_price.value - discount.value; 
	sum();
	
</script>
