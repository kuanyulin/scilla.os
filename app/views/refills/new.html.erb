<div class='at-form'>
<fieldset>
  <legend>新增訂單</legend>

<% form_for(@refill) do |f| %>
  <%= render :partial => 'form_new', :locals => { :f => f } %>
  <p>
    <%= f.submit '新增' %>
  </p>
<% end %>

</div>

<%= link_to '< Back', refills_path %>