<div class='at-form'>
	
<% form_for(@style) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <div>
    <%= f.submit '更新' %>
  </div>
<% end %>

</div>

<%= link_to 'Show', @style %> |
<%= link_to 'Back', styles_path %>
